import React from 'react';

import Navbar from '../../components/navbar/Navbar';

const Courses = props => {
    return(
        <div>
            <Navbar />
            
        </div>
    );
};

export default Courses;